<h1>Users#show</h1>

<%= image_tag @user.picture.url(:medium) if @user.picture.present? %>


<div class="container">
  <div class="row">
    <div class="col-lg-2">
      <%= image_tag @user.picture.url(:medium) if @user.picture.present? %>
    </div>
    <div class="col-lg-2 col-lg-offset-2">
      <p>Name: <strong><%= current_user.full_name %></strong></p>
      <%= link_to 'Edit profile', edit_user_path(current_user), method: :patch %>
    </div>
  </div>

</div>
