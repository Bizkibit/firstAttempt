
<div class="container-fluid">
  <div class="col-lg-2">
    <div class="row">
      <ul class="nav nav-pills nav-stacked">
        <li><a href="#">Menu 1</a></li>
        <li><a href="#">Menu 2</a></li>
        <li><a href="#">Menu 3</a></li>
      </ul>
    </div>
  </div>
  <!-- <div class="col-lg-2">
    <div class="row">
      <%= image_tag @user.picture.url(:medium), class:'img-circle' if @user.picture.present? %>
    </div>
    <div class="row">
      <p>Name: <strong><%= @user.full_name %></strong></p>
      <%# link_to 'Edit profile', edit_user_path(@user), method: :patch %>
      <%= link_to 'Edit profile', edit_user_path(@user)%>
    </div>
  </div> -->
  <div class="col-lg-2 col-lg-offset-1">
    <div class="row">

      <div class="panel panel-default">
        <div class="panel-body">
          <div class="panel panel-default">
            <div class="panel-heading">
              <%= image_tag @user.picture.url(:medium), class:'img-rounded', alt:"Cinque Terre", style: 'min-width:100%; min-height:100%; width:auto; height:auto;' if @user.picture.present? %>
            </div>
          </div>
          <div class="panel panel-default">
            <div class="panel-heading">
              <%= link_to 'Edit profile', edit_user_path(@user)%>
            </div>
          </div>
        </div>
      </div>

    </div>
    <div class="row">
      <div class="panel panel-default">
        <div class="panel-heading">
          Memberships
        </div>
        <div class="panel-body">
            <% @user.memberships.each do |um| %>
            <p><%=link_to um.name, organization_path(um) %></p>
            <% end %>
        </div>
      </div>
    </div>

  </div>
  <div class="col-lg-5">
    <div class="panel panel-default">
      <div class="panel-body">
            <h2><%= @user.full_name %></h2>
            <hr>
            <p><strong>Bio: </strong> <%= @user.bio %></p>
            <hr>
            <p><strong>Member since: </strong> <%= @user.created_at.strftime("%b %Y") %></p>
      </div>
    </div>
  </div>
</div>
